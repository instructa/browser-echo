{
  "name": "vite-browser-logs",
  "version": "0.1.0",
  "description": "Stream browser console logs to the Vite dev server terminal with colors, stack modes, batching, and optional file logging.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "unbuild",
    "prepack": "unbuild",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "oxlint",
    "lint:ci": "oxlint --deny-warnings",
    "lint:fix": "oxlint --fix"
  },
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "types": "./dist/index.d.ts"
    }
  },
  "keywords": [
    "vite",
    "plugin",
    "logs",
    "console",
    "terminal",
    "dev",
    "ansis"
  ],
  "peerDependencies": {
    "vite": "^5 || ^6 || ^7"
  },
  "dependencies": {
    "ansis": "^4.1.0"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^24.2.1",
    "oxlint": "^1.11.1",
    "typescript": "^5.9.2",
    "unbuild": "^3.6.0",
    "vite": "^7.1.1",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@9.12.2"
}
